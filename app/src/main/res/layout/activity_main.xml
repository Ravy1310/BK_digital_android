<?xml version="1.0" encoding="utf-8"?>
<androidx.drawerlayout.widget.DrawerLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/drawer_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="true">

    <!-- =============================== -->
    <!-- MAIN CONTENT AREA -->
    <!-- =============================== -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <!-- HEADER (Menggunakan header.xml yang sudah Anda buat) -->
        <include
            layout="@layout/header"
            android:id="@+id/mainHeader" />


        <!-- Container untuk konten dashboard/fragment -->
        <LinearLayout
            android:id="@+id/fragment_container"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1">

            <!-- Konten default (dashboard) akan dimuat di sini -->
            <include
                android:id="@+id/default_dashboard_content"
                layout="@layout/dasboarddlagii" />

        </LinearLayout>

    </LinearLayout>

    <!-- =============================== -->
    <!-- NAVIGATION DRAWER (SIDEBAR) -->
    <!-- =============================== -->
    <com.google.android.material.navigation.NavigationView
        android:id="@+id/nav_view"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_gravity="start"
        android:fitsSystemWindows="true"
        app:menu="@menu/drawer_menu"
        app:itemTextColor="@color/nav_text_color"
        app:itemIconTint="@color/nav_icon_color" />

</androidx.drawerlayout.widget.DrawerLayout>